apiVersion: v1
kind: Service
metadata:
  name: users-api-service
  namespace: social-media-monorepo
  labels:
    app: users-api
spec:
  selector:
    app: users-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP