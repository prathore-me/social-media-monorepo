apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api-deployment
  namespace: social-media-monorepo
  labels:
    app: users-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
    spec:
      containers:
        - name: users-api-container
          image: prathoreme/social-media-monorepo-users-api:v0.2.0
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: social-media-monorepo-secrets
            - configMapRef:
                name: social-media-monorepo-configmap
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: '128Mi'
              cpu: '100m'
            limits:
              memory: '256Mi'
              cpu: '500m'
